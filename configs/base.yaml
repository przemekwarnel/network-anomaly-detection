data:
  path: data/raw/KDD_Cup_1999.csv.gz
  target_column: outcome
  train_size: 0.7
  val_size: 0.15
  test_size: 0.15
  random_state: 42

model:
  hidden_dims: [32, 16]
  latent_dim: 8

training:
  batch_size: 256
  num_epochs: 20
  learning_rate: 0.001
  criterion: MSELoss
  optimizer: Adam